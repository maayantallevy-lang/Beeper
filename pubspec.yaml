name: alerts
description: Emergency Beeper Application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Firebase Core
  firebase_core: ^2.27.0
  firebase_messaging: ^14.7.15
  flutter_local_notifications: ^17.1.0
  
  # חובה עבור נודניק מדויק (מונע את הבעיה שהתראות לא מופיעות)
  flutter_timezone: ^1.0.8
  
  # Utilities
  shared_preferences: ^2.2.2
  intl: 0.18.1
  uuid: ^4.3.3
  permission_handler: ^11.3.0
  timezone: ^0.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
